import { useState, useEffect } from "react";

export default function Home() {
  const [contests, setContests] = useState([]);

  // tymczasowe dane — później pobierzemy je z backendu
  useEffect(() => {
    setContests([
      { id: 1, name: "Budżet obywatelski Kraków 2025", description: "Głosowanie na projekty miejskie." },
      { id: 2, name: "Zielone Inicjatywy 2025", description: "Pomysły na ekologiczne rozwiązania w mieście." },
      { id: 3, name: "Konkurs Młodych Talentów", description: "Wybierz najlepszy projekt artystyczny." },
      { id: 4, name: "Nowe Technologie dla Miast", description: "Innowacje poprawiające jakość życia mieszkańców." },
      { id: 5, name: "Sport i Aktywność", description: "Projekty promujące zdrowy styl życia." },
    ]);
  }, []);

  return (
    <div className="min-h-screen bg-gray-50 p-8">
      <h1 className="text-3xl font-bold text-center mb-10">Wybierz konkurs</h1>
      <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {contests.map((contest) => (
          <div
            key={contest.id}
            className="bg-white shadow-md rounded-2xl p-6 hover:shadow-lg transition-shadow"
          >
            <h2 className="text-xl font-semibold mb-2 text-gray-800">{contest.name}</h2>
            <p className="text-gray-600 mb-4">{contest.description}</p>
            <a
              href={`/contest/${contest.id}`}
              className="inline-block bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700"
            >
              Przejdź do głosowania
            </a>
          </div>
        ))}
      </div>
    </div>
  );
}
